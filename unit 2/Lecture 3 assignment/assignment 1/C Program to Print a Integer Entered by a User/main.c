#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;
    printf("enter a integer::>> ");
    fflush(stdin);fflush(stdout);
    scanf("%d",&number);
    printf("your number is :::> %d",number);
    return 0;
}
